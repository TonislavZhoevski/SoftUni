function charSequence (text) {
    
    /* for (let i = 0; i < text.length; i++) {
        let letter = text[i];
        console.log(letter);
    } */

    for (let i = 0; i <= text.length - 1; i++) {
        console.log(text[i]);
    }
    
}
charSequence("softuni");